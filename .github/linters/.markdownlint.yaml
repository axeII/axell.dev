---
default: true

# Disable line-length — existing posts have very long lines
MD013: false

# Hugo content files start with YAML frontmatter, not headings
MD041: false

# Inline HTML is used intentionally (Hugo shortcodes, img tags)
MD033: false
